/// <reference types="vite/client" />
/* Vite 客户端环境类型：为 `?worker&inline`（内联 Worker，见 worker/routeClient.ts）、
   `import.meta.env.DEV/PROD`（发行分流，见 ui/main.ts）等 Vite 专属语法提供声明，
   使 `tsc --noEmit` 认得它们。仅类型声明，不入运行时。 */
